# Canonical install: uv sync (see pyproject.toml and uv.lock).
# This file is a minimal pip fallback only — do not add pip.ini / TOML keys here.

httpx>=0.27
python-dotenv>=1.0
requests>=2.32.3
pydantic>=2.0.0
