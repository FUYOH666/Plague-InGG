[project]
name = "ekaterina-seed"
version = "0.0.1"
description = "Seed — the minimum from which Ekaterina v2 grows itself"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "pyyaml>=6.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
